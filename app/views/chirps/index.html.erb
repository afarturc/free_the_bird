<section class="flex flex-col items-center w-full">
    <%= render partial: "chirp_form" if user_signed_in? %>
    <% @chirps.each do |chirp| %>
        <%= render partial: "chirp_card", locals: { chirp: chirp } %>
    <% end %>
</section>
